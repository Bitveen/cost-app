import React from "react";
import ReactDOM from "react-dom";
import TestUtils from "react-addons-test-utils";
import expect from "expect";

import { Menu } from "Menu";

describe("Menu", () => {
    it("should exist", () => {
        expect(Menu).toExist();
    });

    // it("should render itself", () => {
    //     const menu = TestUtils.renderIntoDocument(<Menu />);
    //     const elem = ReactDOM.findDOMNode(menu);
    //     expect(elem.classList.contains("menu")).toEqual(true);
    // });




});